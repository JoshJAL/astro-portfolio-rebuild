---
import BioYear from './BioYear.astro';

export interface Props {
  year: number | string;
  text: string;
}

const { year, text } = Astro.props;
---

<div class='flex gap-4'>
  <BioYear year={year} />
  <p class='md:text-lg'>{text}</p>
</div>
