---
export interface Props {
  label: string;
  extraClasses?: string;
}

const { label, extraClasses } = Astro.props;
---

<span
  class={`bg-orange-200 dark:bg-teal-200 dark:bg-opacity-20 rounded-lg p-2 font-semibold ${
    extraClasses ? ' ' + extraClasses : ''
  }`}>{label}</span
>
